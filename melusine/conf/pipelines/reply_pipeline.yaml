reply_pipeline:
  steps:
  - class_name: MelusinePipeline
    config_key: preprocessing_pipeline
    module: melusine.pipeline
  - class_name: ReplyDetector
    config_key: reply_detector
    module: melusine.detectors
